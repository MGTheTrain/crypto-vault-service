// Package keys defines the core interfaces and repository operations for managing cryptographic keys.
// It provides services for uploading, retrieving, deleting, and downloading cryptographic keys, as well as
// managing their metadata. The repository interface abstracts the persistence layer for interacting with
// cryptographic key data.
package keys
